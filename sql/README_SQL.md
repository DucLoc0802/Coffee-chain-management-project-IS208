# PhungLocCoffeeApp SQL

## Thứ tự chạy script

1. `01_drop_objects.sql`
2. `02_tables.sql`
3. `06_sample_data.sql`

`03_functions.sql`, `04_triggers.sql`, `05_procedures.sql` đang là placeholder để bổ sung sau.

## Cách chạy trong SQL Developer

Đăng nhập schema `pl_coffee`, mở từng file theo đúng thứ tự trên, sau đó chạy toàn bộ nội dung bằng `Run Script`.

## Tài khoản test

- username: `admin`
- password: `123456`

## JDBC URL

`jdbc:oracle:thin:@//localhost:1521/freepdb1`
